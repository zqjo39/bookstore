extends ../layout

block content
    h3= book.title
    a(href = `/books/edit/${book.id}`) Edit Course
    div
        img(src = book.cover_image)
        p Genre: #{book.genre}
        // for author
        p Publisher: #{book.publisher}
        p Page Count: #{book.page_count}
        p= book.description